/**
 * Helper type to pass a simple 2d point.
 */
export type XY = {
  x: number;
  y: number;
};

/**
 * Helper type to pass a simple width and height.
 */
export type Size = {
  width: number;
  height: number;
};
